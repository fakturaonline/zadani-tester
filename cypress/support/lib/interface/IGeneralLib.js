module.exports = class IGeneralLib {

    /**
     * Login user into his account based on his email and password
     * @param email
     * @param password
     */
    login(email, password) {
    }

    /**
     * Visit webpage under the test on desired domain
     * @param domain
     */
    visitPage(domain){
    }

    /**
     * Create a random numerical string based on desired length
     * @param length
     * @return String with numeric values in desired length
     */
    generateRandomNumericalString(length) {
    }
}